# REQUIREapp

React, TypeScript, Vite, and Firebase starter for the `requireapp-b74b3` project.

## Local development

```powershell
npm install
npm run dev
```

## Production build

```powershell
npm run build
```

Firebase Hosting is configured to publish the `dist` directory.
